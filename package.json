{
  "name": "telebridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TeleBridge: Docker-first Telegram MTProto user message relay with Hugging Face Space support.",
  "scripts": {
    "check": "node --check scripts/userbot-config.mjs && node --check scripts/userbot-runtime.mjs && node --check scripts/login-user.mjs && node --check scripts/user-forwarder.mjs && node --test",
    "test": "node --test",
    "user:login": "node scripts/login-user.mjs",
    "user:forward": "node scripts/user-forwarder.mjs"
  },
  "dependencies": {
    "telegram": "^2.26.22",
    "yaeti": "file:vendor/yaeti-compatible"
  }
}
